{
  "name": "truedata-backend-deploy",
  "private": true,
  "version": "1.0.0",
  "description": "Operational tooling for deploying and testing backend on Vercel",
  "scripts": {
    "vercel:login": "vercel login",
    "vercel:deploy": "vercel --prod --confirm",
    "vercel:preview": "vercel --confirm",
    "test:api": "bash ./test-api.sh $DEPLOYMENT_URL"
  },
  "devDependencies": {
    "vercel": "^34.0.0",
    "cross-env": "^7.0.3"
  },
  "engines": {
    "node": ">=18"
  }
}